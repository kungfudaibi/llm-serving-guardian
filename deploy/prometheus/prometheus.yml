global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: llm-serving-guardian
    static_configs:
      - targets: [guardian:8090]
